import { zeroAnswers } from 'ember-proj8/helpers/zero-answers';
import { module, test } from 'qunit';

module('Unit | Helper | zero answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = zeroAnswers([42]);
  assert.ok(result);
});
